// @flow
export type Member = {
  exempt: boolean,
  roles: Array<string>,
  phones: Array<{}>,
  firstName: string,
  lastName: string,
  comments: string,
  email: string,
}

// export default { Member }
